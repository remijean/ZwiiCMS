# Cache le PHPSESSID de l'url
SetEnv SESSION_USE_TRANS_SID 0

# Bloque l'accès à la liste des fichiers
Options -Indexes

# Bloque l'accès aux fichiers json
<FilesMatch "\.json$">
	Order deny,allow
	Deny from all
</FilesMatch>

# Attention, surtout ne rien modifier ci-dessous !
# URL rewriting